import { test, expect } from '../fixtures';

// test('amount', async ({page}) => {
//   await page.goto('https://qa-utsav-patel.rt.gw/wp-admin');  
//   await page.click('text=Form Entries'); 
//   await expect(page.locator('.form_entries_unread')).toHaveAmount(4);

// });

// test('Verify attribute value based on text content', async ({ page }) => {
//     await page.goto('https://example.com');
  
//     await expect(page.locator('#myElement')).toHaveAttributeWithText('data-status', 'not have attribute');
//     await expect(page.locator('#myOtherElement')).toHaveAttributeWithText('aria-label', /submit form/i); // Case-insensitive RegExp match
//   });
  
